name: clean_arch
description: >-
  A command-line tool that scaffolds Flutter projects following Clean
  Architecture — generates the full core layer and feature modules with
  boilerplate files in one command.
version: 1.0.0
repository: https://github.com/4bhisheksharma/clean_arch
homepage: https://github.com/4bhisheksharma/clean_arch
issue_tracker: https://github.com/4bhisheksharma/clean_arch/issues

environment:
  sdk: ^3.8.1

executables:
  clean_arch:

dependencies: {}

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
